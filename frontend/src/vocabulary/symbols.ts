/**
 * Re-exported so the vocabulary editor has one import for both halves of drawing a piece of
 * furniture: the glyph sheet and the lookup that says which glyph a type uses.
 */

export { FurnitureSymbols } from "../furniture-symbols";
export { drawableEntityTypes, furnitureSymbol } from "../furniture";
